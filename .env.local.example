# Copie este arquivo para .env.local e preencha com sua chave real.
# O .env.local é ignorado pelo git — a chave nunca deve ser commitada.
# Gere a chave em: https://aistudio.google.com (Get API key)

GEMINI_API_KEY=coloque-sua-chave-aqui
